{
  "routes": [
    {
      "method": "GET",
      "path": "/prendas",
      "handler": "prendas.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/prendas/count",
      "handler": "prendas.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/prendas/:id",
      "handler": "prendas.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/prendas",
      "handler": "prendas.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/prendas/:id",
      "handler": "prendas.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/prendas/:id",
      "handler": "prendas.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
